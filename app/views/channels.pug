h1 Channels  
  a.btn.btn-primary.float-right(href='/channels/new')
    i.fa.fa-plus
    | &nbsp;&nbsp;Add Channel
table.table
  thead
    tr
      th Name
      th.text-right Edit
  tbody
    if channels && channels.length
      each c in channels
        tr
          td
            a(href=`/channels/${c._id}/edit`)= c.name
          td.text-right
            a(href=`/channels/${c._id}`) Preview
    else
      tr
        td.text-gray(colspan=42) No channels available.